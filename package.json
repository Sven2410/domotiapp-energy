{
  "name": "domotiapp-energy-frontend-tests",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Test-only tooling for the DomotiApp Energy panel: a jsdom suite and two Playwright routes. The integration itself ships no JavaScript dependencies and needs no build step.",
  "license": "MIT",
  "scripts": {
    "test": "node --test \"tests/frontend/**/*.test.mjs\"",
    "test:layout": "playwright test --config tests/browser/playwright.config.mjs",
    "test:live": "playwright test --config tests/browser/live.config.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "jsdom": "^26.1.0"
  }
}
